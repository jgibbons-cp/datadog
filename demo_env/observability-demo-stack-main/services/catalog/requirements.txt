fastapi==0.111.0
uvicorn[standard]==0.30.1
psycopg2-binary==2.9.9
redis==5.0.4
ddtrace==4.13.1
# ddtrace imports wrapt at runtime. If it is missing, sitecustomize.py logs a
# traceback and carries on -- the app works and silently emits no traces.
wrapt>=2.3.0
